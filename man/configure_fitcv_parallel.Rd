% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup_helpers.R
\name{configure_fitcv_parallel}
\alias{configure_fitcv_parallel}
\title{Build the local parallel configuration.}
\usage{
configure_fitcv_parallel(
  n_fit_jobs = 4L,
  default_cv_folds = SIM2_DEFAULT_CV_FOLDS,
  default_cv_nlambda = 50L,
  backend = Sys.getenv("HAL_FIT_BACKEND", "auto"),
  n_runs = 1L,
  default_active_mc_headroom = 1.2
)
}
\arguments{
\item{n_fit_jobs}{Integer number of nuisance-model fit jobs.}

\item{default_cv_folds}{Integer default HAL CV folds.}

\item{default_cv_nlambda}{Integer default number of HAL lambda values.}

\item{backend}{Character dispatch backend for fit-level parallelism:
\code{"auto"}, \code{"fork"}, or \code{"psock"}.}

\item{n_runs}{Integer number of sample-combination-by-MC runs.}

\item{default_active_mc_headroom}{Positive automatic active-window
multiplier. The default \code{1.2} matches the adopted global-queue rule.}
}
\value{
A named list with CPU, worker, CV, and backend settings.
}
\description{
Reads environment overrides such as \code{HAL_TOTAL_CORES}, \code{HAL_FIT_WORKERS},
\code{HAL_MAX_CONCURRENT_HAL_FITS}, \code{HAL_MAX_ACTIVE_MC}, and the HAL CV controls,
then returns the configuration used by the global scheduler. Manual values
are clipped so the complete-fit slots stay within the usable CPU budget.
}
