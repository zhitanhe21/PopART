% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_fitting.R
\name{fit_hal_timed}
\alias{fit_hal_timed}
\title{Fit one HAL model and record timing.}
\usage{
fit_hal_timed(
  label,
  X,
  Y,
  weights = NULL,
  family = "binomial",
  smoothness_orders = 1,
  max_degree = 2,
  num_knots = SIM2_DEFAULT_NUM_KNOTS,
  foldid = NULL,
  cv_folds = SIM2_DEFAULT_CV_FOLDS,
  cv_nlambda = 50,
  cv_workers = 4,
  cv_cluster = NULL,
  cache_file = NULL,
  cache_key = NULL,
  use_cache = TRUE
)
}
\arguments{
\item{label}{Character label for the nuisance fit.}

\item{X}{Data frame or matrix of predictors.}

\item{Y}{Response vector.}

\item{weights}{Optional observation weights.}

\item{family}{Character outcome family passed to \code{hal9001::fit_hal()}.}

\item{smoothness_orders}{HAL smoothness order setting.}

\item{max_degree}{HAL maximum interaction degree.}

\item{num_knots}{HAL knot setting.}

\item{foldid}{Optional vector of CV fold IDs.}

\item{cv_folds}{Integer number of CV folds.}

\item{cv_nlambda}{Integer number of lambda values.}

\item{cv_workers}{Integer number of workers for HAL CV.}

\item{cv_cluster}{Optional reusable CV PSOCK cluster. It is only supported
when complete HAL fits are running serially in the calling R process.}

\item{cache_file}{Optional \code{.rds} cache file path.}

\item{cache_key}{Expected v2 content key for \code{cache_file}.}

\item{use_cache}{Logical, whether to read/write the cache file.}
}
\value{
A list with \code{fit}, the fitted HAL object, and \code{timing}, a one-row data
frame of fit diagnostics.
}
\description{
This is a wrapper around \code{hal9001::fit_hal()}. It does not implement HAL
itself; it manages CV workers, optional fit caching, and timing diagnostics
for one nuisance model.
}
