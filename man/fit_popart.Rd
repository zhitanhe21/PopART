% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_popart.R
\name{fit_popart}
\alias{fit_popart}
\title{Fit PopART estimators to trial and auxiliary data}
\usage{
fit_popart(
  trial_data,
  auxiliary_data,
  outcome,
  treatment,
  response,
  censoring,
  covariates,
  auxiliary_weight = NULL,
  treatment_values = c(0, 1),
  control = popart_control()
)
}
\arguments{
\item{trial_data}{A data frame containing the randomized-trial records.}

\item{auxiliary_data}{A data frame representing the target population. It
needs the requested covariates and, optionally, an auxiliary weight column;
trial-only outcome and missingness columns are not required.}

\item{outcome, treatment, response, censoring}{Single character strings naming
columns in \code{trial_data}. All four variables must be binary. A value of one
for \code{response} means the participant responded; a value of one for
\code{censoring} means their outcome is censored.}

\item{covariates}{Character vector naming numeric or logical baseline
covariates present in both data sets. Categorical variables should be
encoded before calling this function.}

\item{auxiliary_weight}{Optional column in \code{auxiliary_data} containing
nonnegative target-population weights. Unit weights are used when \code{NULL}.}

\item{treatment_values}{Two distinct values in control, treated order. The
default supports a treatment column coded 0/1. For example, use
\code{c("usual care", "intervention")} for labeled arms.}

\item{control}{A control object created by \code{\link[=popart_control]{popart_control()}}.}
}
\value{
An object of class \code{popart_fit} containing estimates, variances,
confidence intervals, nuisance-fit timing, and diagnostic information.
}
\description{
Fits trial-only and trial-and-auxiliary AIPW estimators using four HAL nuisance
models: a shared outcome regression, one censoring regression, and separate
trial-selection models for the control and treated arms. This function
analyzes supplied data; it does not generate data or run Monte Carlo
simulations.
}
\examples{
\dontrun{
fit <- fit_popart(
  trial_data = trial,
  auxiliary_data = auxiliary,
  outcome = "outcome",
  treatment = "arm",
  response = "responded",
  censoring = "censored",
  covariates = c("age", "baseline_score"),
  auxiliary_weight = "survey_weight"
)
summary(fit)
}
}
