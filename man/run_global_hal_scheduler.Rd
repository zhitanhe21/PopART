% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/global_scheduler.R
\name{run_global_hal_scheduler}
\alias{run_global_hal_scheduler}
\title{Run complete HAL fits from multiple MC runs through one shared queue.}
\usage{
run_global_hal_scheduler(
  run_ids,
  prepare_run,
  finalize_run,
  project_dir = NULL,
  global_workers = 1L,
  per_run_workers = 2L,
  max_active_runs = 1L,
  use_cache = TRUE,
  task_fun = global_hal_default_task,
  slots = NULL,
  initialize_hal = TRUE,
  cv_workers_per_fit = 1L,
  poll_interval_ms = 50L,
  verbose = TRUE
)
}
\arguments{
\item{run_ids}{Unique run identifiers in admission order.}

\item{prepare_run}{Function taking one run ID and returning a list with named
\code{jobs}; every other component is retained as the finalization context.}

\item{finalize_run}{Function called as
\code{finalize_run(run_id, context, fit_outputs, error, metrics)}.}

\item{project_dir}{Optional local demo source directory used to initialize
HAL slots. When omitted outside source mode, workers load the installed
\code{aipwLocalDemo} namespace.}

\item{global_workers}{Maximum concurrently running complete HAL fits.}

\item{per_run_workers}{Maximum concurrently running fits from one run.}

\item{max_active_runs}{Maximum prepared, unfinished run contexts.}

\item{use_cache}{Whether workers may use the fit cache.}

\item{task_fun}{Function executed for each job inside a slot.}

\item{slots}{Optional pre-created callr sessions, mainly for tests.}

\item{initialize_hal}{Whether owned slots should load the HAL project.}

\item{cv_workers_per_fit}{Positive integer number of CV workers owned by
each complete-HAL slot.}

\item{poll_interval_ms}{Milliseconds between completion polls.}

\item{verbose}{Whether to print admission/completion progress.}
}
\value{
A list with finalizer return values, an event trace, and observed
concurrency maxima.
}
\description{
Run complete HAL fits from multiple MC runs through one shared queue.
}
