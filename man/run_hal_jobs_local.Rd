% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_fitting.R
\name{run_hal_jobs_local}
\alias{run_hal_jobs_local}
\title{Dispatch multiple HAL nuisance-fit jobs locally.}
\usage{
run_hal_jobs_local(
  job_specs,
  fit_workers,
  backend = "auto",
  use_cache = TRUE,
  fit_cluster = NULL,
  cv_cluster = NULL
)
}
\arguments{
\item{job_specs}{Named list of HAL fit specifications.}

\item{fit_workers}{Integer number of complete HAL fits to run concurrently.}

\item{backend}{Character dispatch backend: \code{"auto"}, \code{"fork"}, or \code{"psock"}.}

\item{use_cache}{Logical, whether cached HAL fits may be used.}

\item{fit_cluster}{Optional reusable outer PSOCK cluster.}

\item{cv_cluster}{Optional reusable CV PSOCK cluster. Only used when
\code{fit_workers == 1}.}
}
\value{
Named list of outputs from \code{fit_hal_timed()}.
}
\description{
Runs the nuisance fits serially, with forked workers, or with a PSOCK cluster
depending on \code{fit_workers} and \code{backend}.
}
