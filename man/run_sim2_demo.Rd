% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim2_demo.R
\name{run_sim2_demo}
\alias{run_sim2_demo}
\title{Run the version-2 local optimized Simulation 2 AIPW/HAL demo.}
\usage{
run_sim2_demo(
  project_dir = NULL,
  scale = "demo",
  run_id = paste0("local_", format(Sys.time(), "\%Y\%m\%d_\%H\%M\%S")),
  base_seed = 91000000L,
  out_dir = file.path(getwd(), "aipw-output"),
  cache_dir = file.path(tools::R_user_dir("aipwLocalDemo", which = "cache"), "hal"),
  use_cache = TRUE,
  resume = TRUE,
  checkpoint_dir = NULL,
  backend = Sys.getenv("HAL_FIT_BACKEND", "auto"),
  m = 20L,
  p_resp = 0.5,
  p_cens = 0.3,
  n_trial = NULL,
  n_aux = NULL,
  max_runs = NULL,
  mc_reps = 1L,
  total_cores = NULL,
  reserve_cores = NULL,
  fit_workers = NULL,
  cv_workers_per_fit = NULL,
  max_concurrent_hal_fits = NULL,
  max_active_mc = NULL,
  active_mc_headroom = NULL,
  cv_folds = NULL,
  cv_nlambda = NULL,
  dry_run = FALSE,
  write_sd_file = FALSE,
  cluster_id = 0L
)
}
\arguments{
\item{project_dir}{Optional character path to a source checkout. Installed
package users can leave this as \code{NULL}; it is retained for compatibility
with the repository CLI and source-based workflows.}

\item{scale}{Character sample grid name: \code{demo}, \code{small}, or \code{large}.}

\item{run_id}{Character identifier used in output file names.}

\item{base_seed}{Integer base seed. Monte Carlo replicate \code{r} uses
\code{base_seed + r} across all requested sample-size combinations, matching the
original Brian Simulation 2 grid organization.}

\item{out_dir}{Character directory for result and timing files.}

\item{cache_dir}{Character directory for optional HAL fit cache files.}

\item{use_cache}{Logical, whether HAL fit caching is enabled.}

\item{resume}{Logical, whether valid successful-run checkpoints may be used.}

\item{checkpoint_dir}{Optional checkpoint directory. The default is a
run-specific directory below \code{out_dir/checkpoints}.}

\item{backend}{Character fit-level backend: \code{auto}, \code{fork}, or \code{psock}.}

\item{m}{Integer number of clusters.}

\item{p_resp}{Numeric response probability setting, fixed at \code{0.5} by the
preserved Simulation 2 DGP.}

\item{p_cens}{Numeric censoring probability setting, fixed at \code{0.3} by the
preserved Simulation 2 DGP.}

\item{n_trial, n_aux}{Optional integers for running one explicit sample-size
pair instead of a named grid.}

\item{max_runs}{Optional integer limiting how many sample-size combinations
are selected before Monte Carlo expansion.}

\item{mc_reps}{Positive integer number of Monte Carlo replicates for every
requested sample-size combination.}

\item{total_cores, reserve_cores, fit_workers, cv_workers_per_fit, cv_folds, cv_nlambda}{Optional local parallel and HAL CV overrides. In the global scheduler,
\code{fit_workers} is the per-run HAL-fit cap.}

\item{max_concurrent_hal_fits}{Optional global complete-HAL-fit slot limit.}

\item{max_active_mc}{Optional bound on prepared unfinished run contexts.}

\item{active_mc_headroom}{Optional positive multiplier for the automatic
active-run window; the default is \code{1.2}.}

\item{dry_run}{Logical, whether to print the plan without fitting HAL models.}

\item{write_sd_file}{Logical, whether to also write \verb{sd<cluster_id>.csv}.}

\item{cluster_id}{Integer identifier used for the optional Brian-style \code{sd}
output file.}
}
\value{
Invisibly returns configuration, paths, timings, and results.
}
\description{
This entry point uses a bounded cross-run global HAL queue, persistent local
worker slots, content-addressed fit caching, successful-run resume
checkpoints, and per-run atomic CSV shards.
}
\examples{
plan <- run_sim2_demo(
  run_id = "demo_plan",
  out_dir = file.path(tempdir(), "aipw-demo-plan"),
  cache_dir = file.path(tempdir(), "aipw-demo-cache"),
  use_cache = FALSE,
  resume = FALSE,
  max_runs = 1,
  total_cores = 2,
  dry_run = TRUE
)
}
